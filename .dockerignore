.git
.github
.venv
.pytest_cache
.ruff_cache
__pycache__
*.py[cod]
dist
build
tests
Dockerfile
.dockerignore
.pre-commit-config.yaml

# meter-driver-spec is deliberately NOT ignored: the builder stage needs the
# submodule to generate openapi.json. Only the wheel is copied into the final
# stage, so the spec never reaches the shipped image.
